<Window x:Class="GPXManager.views.EditBrandModelWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GPXManager.views"
        mc:Ignorable="d"
        Title="EditBrandModelWindow" Height="163.628" Width="433.179" ResizeMode="NoResize">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition Width="130"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="1">
            <Button Content="OK" Height="25" Margin="20 30 20 20" x:Name="buttonOK" Click="OnButtonClick"/>
            <Button Content="Cancel" Height="25" Margin="20 0" x:Name="buttonCancel" Click="OnButtonClick"/>
        </StackPanel>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition Height="2*"/>
            </Grid.RowDefinitions>
            <Label Content="This is a label" x:Name="labelTitle" VerticalAlignment="Bottom" Margin="20 0"/>
            <TextBox Grid.Row="1" x:Name="textBox" Margin="20 10 20 40" Height="25" VerticalAlignment="Top" VerticalContentAlignment="Center"/>
        </Grid>
    </Grid>
</Window>
